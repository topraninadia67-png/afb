# 三亚千古情艺术团管理小程序 · 部署指南

本项目是一个**微信小程序 + 云开发**应用，无需自建服务器。按以下步骤即可上线给团员使用。

---

## 一、准备工作（约 20 分钟，需你本人操作）

1. **注册小程序账号**
   - 打开 https://mp.weixin.qq.com → 「立即注册」→ 选择「小程序」。
   - 个人 / 企业均可（企业可上架更多能力，个人也能用）。
   - 注册完成后，在「开发 → 开发管理 → 开发者ID」中找到 **AppID**，记下来。

2. **下载微信开发者工具**
   - https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
   - 安装后用注册小程序的微信扫码登录。

---

## 二、导入项目并配置

1. 打开微信开发者工具 → 「导入项目」。
2. 目录选择本仓库根目录（包含 `miniprogram/` 和 `cloudfunctions/` 的那一层）。
3. AppID 填写第一步拿到的 **AppID**（不要用测试号，云开发需要正式 AppID）。
4. 导入后，点击工具栏「云开发」按钮 → 开通云开发（首次免费，选默认环境即可）。
5. 开通后会得到一个**环境 ID**，复制它。
6. 打开 `miniprogram/app.js`，把这一行的 `'your-env-id'` 替换成你的环境 ID：
   ```js
   cloudEnv: 'your-env-id',   // ← 换成你的云开发环境 ID
   ```

---

## 三、部署云函数（后端）

在开发者工具左侧文件树，找到 `cloudfunctions` 下的三个函数，**逐个右键 → 「上传并部署：云端安装依赖」**：

- `init`   —— 初始化数据库
- `member` —— 团员业务
- `admin`  —— 管理员业务

> 第一次上传会自动安装 `wx-server-sdk` 依赖，请耐心等待每个函数显示「上传成功」。

---

## 四、初始化数据（只做一次）

1. 右键 `init` 云函数 → 「云端测试」。
2. 请求参数填（可自定义管理员密码，不填默认 `qgq2026`）：
   ```json
   { "adminPassword": "你想要的管理员密码" }
   ```
3. 点击「运行」，返回 `{ "ok": true, ... }` 即表示数据库、示例制度、题库、培训资料、管理员密码都已就绪。

> ⚠️ 管理员密码请务必记好。之后可在 App「管理员后台 → 内容管理 → 设置」里随时修改。

---

## 五、设置数据库权限（重要 · 保护匿名与隐私）

进入「云开发控制台 → 数据库」，对以下集合分别设置**权限**（点集合 → 权限设置 → 自定义安全规则 / 选预设）：

| 集合 | 建议权限 | 说明 |
|------|----------|------|
| `suggestions`（意见箱） | **仅创建者可读写**，或「所有用户不可读，仅管理端云函数可读」 | 保证匿名意见普通人看不到，只有管理员后台（云函数）能看 |
| `exam_records`（成绩） | 仅创建者可读写 | 团员只能看到自己的，汇总由管理员云函数统一读取 |
| `daily_checks`（检查） | 仅创建者可读写 | 同上 |
| `deductions`（扣分） | 所有人可读、仅管理员写 | 团员需查自己扣分 |
| `rules` / `quiz` / `training` | 所有人可读 | 制度、题库、培训公开可查 |
| `config`（密码/令牌） | **所有人不可读写** | 仅云函数可访问，保护管理员密码 |

> 简化做法：除 `rules/quiz/training/deductions` 设为「所有用户可读」外，其余全部设为「仅创建者可读写」。管理员相关数据一律通过 `admin` 云函数读取，云函数拥有管理员权限，不受上述限制。

---

## 六、预览与发布

- **真机预览**：点工具栏「预览」，用手机微信扫码体验。
- **正式发布**：点「上传」→ 在 mp.weixin.qq.com「版本管理」中提交审核 → 审核通过后「发布」。
- 发布后，团员在微信「搜索小程序名称」或扫码即可使用。

---

## 七、给团员 / 管理员的使用说明

**普通团员**：打开小程序 → 填写姓名 → 即可使用日常检查、制度查询、扣分查询、培训考试、匿名意见箱。

**管理员**：首页底部「🔐 管理员入口」→ 输入管理员密码 → 查看全部检查汇总、匿名意见、考试成绩、录入扣分、管理制度题库。

---

## 八、把示例内容换成真实资料

示例制度和题目在 `cloudfunctions/init/seed.js`，也可以**直接在 App 管理员后台**操作：

- 「内容管理 → 制度」：增删制度条款
- 「内容管理 → 题库」：增删考试题目（单选 / 判断）
- 「内容管理 → 培训」：增加培训资料

你把真实的制度文件和安全培训题目发我，我可以帮你批量整理进 `seed.js`，重新初始化即可。
